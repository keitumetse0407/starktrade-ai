# StarkTrade AI — Railway Backend Config
# Deploy: railway up

[build]
builder = "nixpacks"

[deploy]
healthcheckPath = "/api/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10

[variables]
PORT = "8000"
ENVIRONMENT = "production"
DEBUG = "false"
